In this project we learned the syntax of how we can create a static website using s3 bucket. 

how we can get custom outputs that we want from the project.

we can use terraform command line to get  a particular output-> terrafrom output <output_name>

Things to Learn ->

jsonencode -> for passing json
[NeedToLearn] filemd5 ->

Depends on -> this is used to create a explict condition where creation of one resource depends on another resource